Config(
  x: Fraction(0.5),
  y: Fraction(0.25),
  width: Absolute(720),
  height: Absolute(0),
  hide_icons: false, 
  ignore_exclusive_zones: true, 
  layer: Overlay, 
  hide_plugin_info: true, 
  close_on_click: true,
  show_results_immediately: true,
  max_entries: None,
  plugins: [
    "librink.so",
    "libapplications.so",
    "libkidex.so",
    "libsymbols.so",
    "libstdin.so",
    "libshell.so",
    "libtranslate.so",
    // "librandr.so",
  ],
)
